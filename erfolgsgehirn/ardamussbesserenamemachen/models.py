from django.db import models


# Create your models here.
# class Classname(models.Model):
    #collumn1 of table = models. datatype(CharField(nessesary argument: max_length), DateTimeField, IntegerField) datatypes have many optional arguments like default=0 for ints
    #                    models.FroeignKey(other class)

                        #can have many SQL datatypes
    #classes can have classfunctions which work as usual